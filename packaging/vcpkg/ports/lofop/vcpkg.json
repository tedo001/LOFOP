{
  "name": "lofop",
  "version": "1.3.0",
  "description": "C++ inference SDK for LOFOP-Detect object detection models. Runs an exported ONNX model with no Python or PyTorch in the deployed artifact.",
  "homepage": "https://github.com/tedo001/LOFOP",
  "documentation": "https://github.com/tedo001/LOFOP/blob/main/docs/cpp-sdk.md",
  "license": "Apache-2.0",
  "supports": "!uwp",
  "dependencies": [
    {
      "name": "vcpkg-cmake",
      "host": true
    },
    {
      "name": "vcpkg-cmake-config",
      "host": true
    }
  ],
  "features": {
    "onnxruntime": {
      "description": "Enable the ONNX Runtime execution backend, so models can be loaded by path. Without it you supply your own Engine implementation.",
      "dependencies": [
        "onnxruntime"
      ]
    }
  }
}
